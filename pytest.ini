[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: exercises one or more real infrastructure dependencies
    postgres: requires the dedicated cerno_test PostgreSQL database
    chroma: uses a real temporary ChromaDB collection
    stockfish: executes a real Stockfish binary
    slow: integration work that is intentionally excluded from fast feedback
